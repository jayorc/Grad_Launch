node_modules
.next
dist
coverage
.env
.DS_Store
*.log

# TypeScript build cache files are generated locally.
*.tsbuildinfo

# Runtime artifacts. These can contain cookies, resumes, screenshots, portal
# state, or local email receipts, so never add them to source.
storage/
apps/api/storage/
