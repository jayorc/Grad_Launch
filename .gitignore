node_modules
.next
dist
coverage
.env
.DS_Store
*.log

# TypeScript build cache files are generated locally.
*.tsbuildinfo

# Runtime browser/application artifacts. These can contain cookies, screenshots,
# temporary resumes, and portal state, so they should not be added to source.
apps/api/src/storage/
storage/browser/
storage/browser-profile/
storage/logged-browser-profile
